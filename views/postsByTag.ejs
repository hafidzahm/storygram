<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Posts by Tag</title>
    <link href="https://unpkg.com/flowbite@1.4.0/dist/flowbite.min.css" rel="stylesheet" />
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
</head>
<body>
    <%- include('_navbar') %>
    <p class="text-center text-2xl font-bold text-gray-900 dark:text-white">Posts for Tag: <%= data.tagName %></p>

    <div class="container mx-auto mt-5 grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4">
        <% data.Posts.forEach(post => { %>
            <div class="max-w-sm rounded overflow-hidden shadow-lg">
                <img class="w-full" src="<%= post.picturePost %>" alt="<%= post.titlePost %>">
                <div class="px-6 py-4">
                    <div class="font-bold text-xl mb-2"><%= post.titlePost %></div>
                    <p class="text-gray-700 text-base">
                        <%= post.captionPost %>
                    </p>
                </div>
                <div class="px-6 pt-4 pb-2">
                    <span class="inline-block bg-gray-200 rounded-full px-3 py-1 text-sm font-semibold text-gray-700 mr-2 mb-2">Tag: <%= data.tagName %></span>
                </div>
                <p class="text-purple-600 dark:text-purple-400">Dibuat Pada: <%= new Date(post.createdAt).toLocaleDateString() %></p>
            </div>
        <% }) %>
    </div>

    <script src="https://unpkg.com/flowbite@1.4.0/dist/flowbite.min.js"></script>
</body>
</html>